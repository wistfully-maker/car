# 2026-08-14 车端可运行联调基线

## 结论

- 车端来源：`ucar@172.20.10.3:/home/ucar/ucar_ws/src`
- 采集时间：2026-08-14 11:47（Asia/Shanghai）
- 车端 `/home/ucar/ucar_ws` 不是 Git 仓库，因此车端没有可确认的分支名。
- 本地旧分支 `feature/task-orchestrator` 只覆盖第一部分，不是当前完整联调版本。
- 本快照从 `codex/phase3-line-follow-integration` 的 `6db83ea` 派生，分支为
  `codex/vehicle-working-baseline-20260814`。
- 当前基线包含第一、二、三部分的车端可运行源码；Gazebo 仿真任务尚未接入。

## 同步范围

以下项目自有包按车端内容同步：

- `task_orchestrator`
- `stop`
- `line_follow_integration`
- `qr_item_search`
- `llm_spark`
- `ucar_avoid`
- `ucar_nav`

其中 `task_orchestrator` 与派生基线内容一致。`qr_item_search` 是权威持续扫码实现，车端在该实现上
把 `scan_window` 调为 1.0 秒。车端备份文件、`.orig`、`.bak`、缓存、日志和生成物未纳入 Git。

以下是车端/设备侧外部依赖，不在本次 Git 快照中：`speech_command`、`ucar_fast_nav`、
`ucar_controller`、`ydlidar`、`usb_cam`、YOLO 模型以及语音密钥文件。若以后直接修改这些路径，
必须另做备份；本分支不能恢复它们。

## 已记录的车端调参差异

- 第三部分起点 `line_start_goal.x = 0.4167081260031493`。
- 左/右 V5 停车确认帧数为 33，停车线搜索仍允许 PID 速度上限 0.48；没有 0.15 的后线搜索限速。
- 直行脚本停车确认帧数为 33；右转 V4 的微调旋转为 14 度。
- 第二部分第二航点为 `x = 0.721455`，Stage 2 逼近速度为 0.2。
- 二维码单点驻留 `scan_window = 1.0` 秒。
- `ucar_nav` 保留车端当前 move_base/TEB 配置和 `cmd_vel_topic` remap。
- `ucar_avoid` 保留车端当前视觉与避障源码，不以较旧本地副本覆盖。

关键运行文件从本分支恢复时应得到的 SHA-256 见同目录下
`2026-08-14-172.20.10.3-runtime.sha256`。Git 对 4 个原为 CRLF 的车端文本文件进行了 LF
归一化，文件逻辑不变，因此该清单以 Git 提交中的可恢复字节为准，而不是以 SCP 原始字节为准。

## 恢复规则

1. 只从本分支的提交恢复，不从根工作区或旧 `feature/task-orchestrator` 分支恢复。
2. 覆盖车端前，先给目标包做带时间戳的目录备份。
3. 只恢复已损坏的包或文件，不整包覆盖外部依赖、密钥、模型、`build`、`devel` 和日志。
4. 恢复后在车端重新执行对应 catkin 编译并重新 source，再做无运动接口检查和人工看护实车测试。
5. 若只是 Gazebo 联调改坏，应优先恢复未来的 Gazebo 接入提交，不要回滚本快照中的实车调参。

建议的本地恢复来源：

```powershell
git -C "D:\program_sec\智能车\.worktrees\vehicle-working-baseline-20260814" show --stat
```

恢复某个文件到临时目录核对后再传车端，例如：

```powershell
git -C "D:\program_sec\智能车\.worktrees\vehicle-working-baseline-20260814" show HEAD:ucar_ws/src/stop/scripts/mission_orchestrator.py > mission_orchestrator.py
scp -i "C:\Users\李冠桥\.ssh\id_rsa" mission_orchestrator.py ucar@172.20.10.3:/tmp/
```

不要直接把未核对的整个 `src` 覆盖到小车。

## 本地校验记录

- `task_orchestrator`：323 项通过，31 项跳过。
- `stop`：113 项通过。
- `line_follow_integration`：69 项通过。
- `qr_item_search`：284 项通过（测试时显式加入包的 `src` 目录）。
- `llm_spark`：11 项通过。
- 七个同步包的 Python `compileall` 通过。
- `ucar_nav` 的 59 项旧测试在派生前后均为相同结果：2 项失败、3 项错误，原因是该包已有测试
  与其当前 footprint、控制超时及 lateral mode 函数签名不一致；本次车端同步没有新增这组失败。

以上是 Windows 本地静态/单元校验，不等于重新编译或重新实车测试；本次同步没有修改小车。
